import React from "react";
import { Wrapper } from "./styles";

const NoProject = () => {
  return (
    <Wrapper>
      <h4>No project found</h4>
      <p>Click on add project to create a project.</p>
    </Wrapper>
  );
};

export default NoProject;
