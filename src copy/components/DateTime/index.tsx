import React from "react";
import DateTimePicker, { DatetimepickerProps } from "react-datetime";

import "./styles.css";

const DateTime = (props: DatetimepickerProps) => <DateTimePicker {...props} />;

export default DateTime;
