import styled from "styled-components";
import { gts } from "../../utils/styles";

export const StyledSortWrapper = styled.div`
  margin: ${gts("mdMargin")}px 0;
  label {
    display: inline-block;
    margin-right: ${gts("smMargin")}px;
  }
`;
